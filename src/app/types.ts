export type DivinationResult = {
  upperTrigram: string
  lowerTrigram: string
  changingLine: string
  hexagramNumber: number
  hexagramName: string
  hexagramContent: string
}

export type FormValues = {
  lowerTrigram: string
  upperTrigram: string
  changingLine: string
} 